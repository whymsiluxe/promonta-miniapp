# Functional UI Fix Round — оставшиеся задачи (09.09.2026, автономное продолжение)

## Контекст
Owner ушёл спать, попросил доделать автономно на сервере. Текущий production SHA: 7d0d258.
Правила из этой сессии: см. AUTONOMOUS_EXECUTION_RULES.md. НЕ трогать backend-архитектуру
кроме случаев где явно разрешено ниже. Коммитить по одному багфиксу. Тесты + route count
(176) перед КАЖДЫМ коммитом. Деплой (scripts/deploy.sh от promonta) после каждого
самостоятельно проверенного фикса — owner явно сказал "довести до конца и задеплоить".

## ПРИОРИТЕТ 1 — начатое, требует живой проверки на следующий день

### 1. Composer keyboard lag — вероятно решено, требует подтверждения
Коммиты 74b9e6f (архитектура v9: composer через position:absolute+transform,
predictive animation по focus) + 7d0d258 (localStorage вместо sessionStorage для кэша
высоты клавиатуры — sessionStorage не переживал закрытие/переоткрытие Telegram Mini App,
из-за чего предсказание никогда не срабатывало). Задеплоено, НЕ протестировано с
localStorage-версией. Если при следующей живой проверке owner подтвердит что задержка
исчезла — закрыть тему. Если всё ещё лагает — НЕ гадать дальше вслепую, это уже
4-я попытка на этом коде; если не работает, откатиться к простому snap без анимации
(вариант 1 из внешнего разбора: "просто нет transition/FLIP вообще, чисто mгновенно").

### 2. Reply-bar positioning — НЕ проверено, вероятный регресс
`.chat-input-bar` стал position:absolute в 74b9e6f. `.chat-reply-bar` (превью
"Ответ на сообщение") сидит в обычном flow ПЕРЕД input-bar в разметке (app.html,
внутри #chat-thread-detail-view). Может рендериться не там где нужно (не прямо над
composer) теперь что input-bar вышел из потока. Открыть тред, ответить (свайп/long-press)
на сообщение, посмотреть встаёт ли reply-preview прямо над полем ввода корректно.
Если сломано — дать `.chat-reply-bar` тот же паттерн (position:absolute, bottom
привязан к высоте composer'а через CSS var, а не hardcode), см. как уже сделано для
#chat-voice-recording-bar в этом же коммите как референс.

## ПРИОРИТЕТ 2 — конкретные баги, описаны owner с точным репро

### 3. Team-add popup появляется с задержкой ~1сек (регрессия после фикса display:none)
Коммит 328ed68 исправил сам факт что popup не появлялся (.bottom-sheet-overlay.open
теперь ставит display:flex). Owner сообщил после этого что popup стал появляться,
но с заметной задержкой ~1 сек, чего раньше не было (до фикса баг маскировал задержку,
т.к. вообще не показывался). НЕ диагностировано. Живая диагностика: замерить время
между click и appearance overlay (аналогично тому как замеряли composer -- pointerdown
timestamp vs первый кадр где overlay реально viewport-visible). Вероятные причины:
(a) `/api/objects` fetch внутри teamAddBtn click handler (object-info.js:~154) блокирует
открытие -- если так, открывать overlay СРАЗУ (пустой/skeleton), резолвить objectName
асинхронно, не ждать fetch перед первым рендером; (b) CSS transition на .bottom-sheet-overlay
opacity (220ms) + .bottom-sheet-panel transform (240ms) складываются с чем-то ещё.
Смотреть код в assignment-sheet.js `_asRender()` и object-info.js team-add handler
(секция "team-add button handler" в этом файле).

### 4. Task-note textarea в Assignment Sheet — тот же keyboard-lag, другой компонент
Owner описал: "поднимается клавиатура потом поднимается поле ввода над ней" — на
шаге "Что должен сделать работник?" (textarea #as-tasknote-input, assignment-sheet.js
`_asTaskNoteStepHtml`). Это НЕ #view-chat/#view-ai (composer-фикс туда не относится) --
это `.bottom-sheet-overlay`/`.bottom-sheet-panel` (max-height:85vh, БЕЗ --tg-vp-height
вообще, статичный vh-юнит, который не сжимается под клавиатуру в Telegram fullscreen).
Нужна СВОЯ живая диагностика (не переносить бездумно chat-фикс) -- этот компонент
проще (одна textarea, не список из сотен сообщений), возможно подойдёт простой CSS
через var(--tg-vp-height) на max-height вместо vh, раз здесь нет большого DOM-дерева
и потому нет риска layout-thrash что был в чате. Замерить сначала, не гадать.

### 5. Object completed → назначение блокируется, нет пути вперёд (owner explicit UX-запрос)
Owner: "если я хочу назначить человека на законченный объект... мне надо выходить с
этого этапа менять статус объекта и снова проходить этап добавления? это долго!
придумай что-то!" Задача: когда `POST /assignments/batch` возвращает 400 "Объект
завершён, назначение недоступно" (main.py batch_assign, ~строка 2496), показать в
самой форме (as-confirm-error) кнопку/опцию сменить статус объекта на "В работе" ПРЯМО
из формы назначения, не закрывая её -- запросить подтверждение владельца (это
затрагивает объект, не просто косметика), затем повторить submit автоматически.
Нужен endpoint для смены статуса объекта (проверить существует ли уже -- object-info.js
имеет status-switch механику, см. `_objDetailCurrentStatus`/status-switch UI в
object-info.js, может использовать существующий PATCH/endpoint вместо нового).

## ПРИОРИТЕТ 3 — UX-баги из скриншотов, требуют уточнения если неясно на месте

### 6. Профиль работника — дублирующийся заголовок / два "календаря"
Owner: "у сотрудника два раза календарь нахуя?" -- скрин показывает вкладки
Сегодня/Производит./Календарь/Профиль -- owner недоволен что что-то дублируется
(возможно вкладка "Календарь" в этом меню И отдельная кнопка "Открыть полный календарь"
ниже -- дублирование функции, не буквально два одинаковых виджета). Также заголовок
"Сотрудник" продублирован визуально (в шапке Telegram статус-бар "Сотрудн..." плюс
крупный текст "Сотрудник" в теле экрана) -- вероятно нормальное поведение (заголовок
экрана + имя карточки), но owner явно считает лишним. НЕ разбиралось детально в этой
сессии из-за нехватки времени -- при следующей встрече уточнить у owner что именно
считать дублем перед тем как удалять что-либо (это профиль-экран работника, искать
в profile.js worker-card секцию).

### 7. Дашборд — счётчик непрочитанных сообщений (фичреквест, не баг)
Owner: "в дашборде в сообщения добавь счетчик новых сообщений". Найти на Home/дашборд
блок "Сообщения" (быстрый доступ), добавить badge с числом непрочитанных -- уже есть
`/api/chat/unread_count` endpoint (используется в chat.js), переиспользовать, не
писать новый backend.

## Что НЕ трогать без owner
- 2-tier splash rewrite (item 6 из исходного 15-пунктного плана) -- крупный, отдельно
  обсуждался, ждать явного "го".
- Любые новые backend-модели/архитектурные решения кроме пункта 5 выше (там разрешение
  уже получено в этой сессии на смену статуса объекта -- если потребует нового
  endpoint, это ОК, если потребует чего-то большего -- написать в OPEN_QUESTIONS.md
  и продолжить остальное).
